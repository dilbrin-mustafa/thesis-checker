# Frontend — scaffolded in Phase 2 (week 11 vertical slice)

Planned: React + TypeScript + Vite + Tailwind + shadcn/ui, PDF highlighting
via `react-pdf`/PDF.js, bilingual (PL/EN) strings under `src/i18n/*.json`.

> Guardrail (doc 02 §13): a CI test will scan `src/i18n/*.json` for
> accusatory phrasing ("plagiarism detected", "wykryto plagiat", ...).
> Never render verdicts — only signals with confidence and evidence.
